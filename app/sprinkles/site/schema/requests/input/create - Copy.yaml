---
teamHome:
  validators:
    required:
      label: "&TEAMHOME"
      message: VALIDATE.REQUIRED
map:
  validators:
    required:
      label: "&MAP"
      message: VALIDATE.REQUIRED
teamAway:
  validators:
    required:
      label: "&TEAMAWAY"
      message: VALIDATE.REQUIRED
pw:
  validators:
    length:
      label: "&PW"
      min: 0
      max: 16
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
winner:
  validators:
    required:
      label: "&WINNER"
      message: VALIDATE.REQUIRED
score:
  validators:
    required:
      label: "&SCORE"
      message: VALIDATE.REQUIRED
    length:
      label: "&SCORE"
      min: 3
      max: 3
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
patch:
  validators:
    required:
      label: "&PATCH"
      message: VALIDATE.REQUIRED
playDate:
  validators:
    required:
      label: "&PLAYDATE"
      message: VALIDATE.REQUIRED
    length:
      label: "&PLAYDATE"
      min: 10
      max: 10
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
gameId:
  validators:
    length:
      label: "&GAMEID"
      min: 0
      max: 16
      message: VALIDATE.LENGTH_RANGE
  transformations:
  - trim
matchType:
  validators:
    required:
      label: "&MATCHTYPE"
      message: VALIDATE.REQUIRED
hiddenGameBans:
  validators:
    required:
      label: "&HIDDENGAMEBANS"
      message: VALIDATE.REQUIRED
hiddenGamePicks:
  validators:
    required:
      label: "&HIDDENGAMEBANS"
      message: VALIDATE.REQUIRED
